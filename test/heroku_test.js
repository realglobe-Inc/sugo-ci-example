/**
 * Test case for heroku.
 * Runs with mocha.
 */
'use strict'

const heroku = require('../lib/heroku.js')
const assert = require('assert')
const co = require('co')

describe('heroku', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Heroku', () => co(function * () {

  }))
})

/* global describe, before, after, it */
