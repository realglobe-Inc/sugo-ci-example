/**
 * Test case for compile.
 * Runs with mocha.
 */
'use strict'

const compile = require('../lib/compile.js')
const assert = require('assert')
const co = require('co')

describe('compile', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Compile', () => co(function * () {

  }))
})

/* global describe, before, after, it */
